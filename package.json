{
	"name": "coloc",
	"version": "0.0.1",
	"private": true,
	"scripts": {
        "prepare-repository": "npm i -g react-native-cli yarn;yarn install; react- native link",
        "emulator": "/home/alex/Android/Sdk/tools/emulator -avd Nexus_5X_API_23 -use-system-libs",
        "install": "react-native run-android",
        "start": "react-native start --reset-cache",
        "android": "npm run install && npm run start",
        "react-devtools": "react-devtools",
        "android-devtools": "adb shell input keyevent 82",
        "clean": "npm cache clean --force && cd android && ./gradlew clean && cd ..",
        "test": "jest"
	},
	"dependencies": {
		"add": "^2.0.6",
		"axios": "^0.17.1",
		"react": "16.0.0",
		"react-native": "0.50.4",
		"react-navigation": "^1.0.0-beta.21"
	},
	"devDependencies": {
		"babel-jest": "21.2.0",
		"babel-preset-react-native": "4.0.0",
		"jest": "21.2.1",
		"react-devtools": "^3.0.0",
		"react-native-cli": "^2.0.1",
		"react-test-renderer": "16.0.0",
		"standard": "^10.0.3"
	},
	"jest": {
		"preset": "react-native"
	}
}
